<?php

/**
 * Provide a list of project existing entities and the ability to add more.
 * Tabs provide field and display admin management.
 */

function brilliant_pr_project_clients_info_page() {
  global $user;

  $content = 'clients';
  $test = $content;
  return $test;
}
